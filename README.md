# Python-Calculator-Using-Functions-and-Dictionary-Mapping
A simple command-line calculator built in Python that performs basic arithmetic operations (+, −, ×, ÷) using functions and a dictionary for operator-to-function mapping. The program takes user input, executes the selected operation, and allows continuation with the previous result.
